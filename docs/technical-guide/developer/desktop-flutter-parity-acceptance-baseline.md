---
title: Desktop Flutter Parity Acceptance Baseline
desc: Executable acceptance baseline for workflow-level Flutter desktop parity validation.
---

# Desktop Flutter Parity Acceptance Baseline

This baseline defines the executable acceptance gate expected for each workflow row in the
[Desktop Flutter Parity Checklist](/technical-guide/developer/desktop-flutter-parity-checklist/).

No workflow row can be marked `Done` until its Flutter gate is implemented, executed, and recorded in
the [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/).

## Related artifacts

- [Web + MCP + Desktop Documentation Map](/technical-guide/developer/web-mcp-documentation-map/)
- [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/)
- [Desktop Flutter Parity Checklist](/technical-guide/developer/desktop-flutter-parity-checklist/)
- [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/)
- [Desktop Flutter Development Runbook](/technical-guide/developer/desktop-flutter-development-runbook/)
- [Desktop Flutter Auth Backend Contract Integration Plan](/technical-guide/developer/desktop-flutter-auth-backend-contract-integration-plan/)
- [Desktop Flutter Release Validation Baseline](/technical-guide/developer/desktop-flutter-release-validation-baseline/)

## Gate status legend

- **Blocked**: Flutter workspace/module dependency not ready.
- **Planned**: gate definition exists, implementation not started.
- **In progress**: test harness exists, still failing or incomplete.
- **Ready**: gate passes in local/CI target matrix.
- **Verified**: gate plus execution-log evidence reviewed and accepted.

## Workflow acceptance baseline

| Workflow domain | Current evidence anchor | Required Flutter acceptance gate | Initial gate target | Status | Owner | Evidence |
|---|---|---|---|---|---|---|
| Authentication/session | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Auth/session UI`) | Sign-in, session restore, token refresh parity integration test (including cyclic backend error-container safety, deep envelope-chain handling, cyclic-primary-wrapper fallback, and sibling `result`/`data` fallback for metadata-only primary wrappers) aligned with [Desktop Flutter Auth Backend Contract Integration Plan](/technical-guide/developer/desktop-flutter-auth-backend-contract-integration-plan/) | `desktop/test/parity/auth_session_parity_test.dart` via `flutter test` | In progress | Auth + Desktop Integration | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Project lifecycle | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Project lifecycle`) | Open/switch project parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/project_lifecycle_parity_test.dart` via `flutter test` | In progress | Workspace Navigation | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| File lifecycle | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`File lifecycle`) | File CRUD/navigation parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/file_lifecycle_parity_test.dart` via `flutter test` | In progress | Workspace Navigation | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Canvas editing | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Canvas interaction`) | Canvas edit semantics parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/canvas_editing_parity_test.dart` via `flutter test` | In progress | Workspace Core | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Asset management | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Asset management`) | Asset import/apply parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/asset_management_parity_test.dart` via `flutter test` | In progress | Workspace Core | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Collaboration context | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Collaboration context`) | Presence/thread continuity parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/collaboration_context_parity_test.dart` via `flutter test` | In progress | Collaboration + Realtime | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Inspect/code handoff | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Inspect/code handoff`) | Inspect metadata/code handoff parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/inspect_handoff_parity_test.dart` via `flutter test` | In progress | Inspect/Handoff | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Export workflows | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Export UX`) | Export option/output/save parity integration test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/export_workflow_parity_test.dart` via `flutter test` | In progress | Export Pipeline | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Diagnostics/recovery | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Diagnostics/recovery UX`) | Runtime health/reconnect/remediation parity test (including sibling `result`/`data` envelope fallback regression for metadata-only primary wrappers) | `desktop/test/parity/diagnostics_recovery_parity_test.dart` via `flutter test` | In progress | Platform Reliability | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |
| Contract runtime mode | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) (`Desktop shell/runtime`) | Runtime mode switch parity test (`in-memory` vs `remote-stub`) with status-surface verification | `desktop/test/parity/remote_stub_mode_parity_test.dart` via `flutter test` | In progress | Desktop Flutter Program | [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/) |

## Baseline execution protocol

1. Define or update the Flutter gate implementation for one workflow row.
2. Run the gate on target platforms (minimum: local primary platform + CI matrix platform).
3. Record run command/result and review notes in the Phase C execution log.
4. Update checklist/inventory status and evidence links in the same implementation unit.

## CI execution anchor

- Desktop parity CI baseline workflow: `.github/workflows/tests-desktop-flutter.yml`.
- CI matrix baseline now runs Linux parity chain, macOS parity+build chain, and Windows parity chain.
- CI now uploads per-platform verification logs and macOS debug app artifact from parity workflow.
- Canonical desktop verification chain script: `desktop/scripts/verify_desktop.sh`.
- Local fast verification path is available through `SKIP_PUB_GET=1` (`desktop:verify:fast`, `desktop:verify:full:fast`) when dependency lock is unchanged.
- Canonical parity test-file list is managed in `desktop/scripts/run_parity_tests.sh` and consumed by root `desktop:test:parity` script + CI workflow.
- Contract-mode matrix script is managed in `desktop/scripts/run_mode_matrix_tests.sh` and consumed by root `desktop:test:mode-matrix` script + verification chain.
- Canonical parity list includes cross-cutting navigation persistence gate: `desktop/test/parity/shell_contract_persistence_parity_test.dart`.
- Canonical parity list includes cross-cutting runtime mode gate: `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Canonical parity list includes remote-stub unavailable-profile gate: `desktop/test/parity/remote_stub_unavailable_parity_test.dart`.
- Remote-stub degraded-path simulation now supports operation-scoped blocking via `PENJAR_DESKTOP_REMOTE_STUB_BLOCKED_OPERATIONS` (comma-separated operation IDs) in addition to global unavailable profile flag `PENJAR_DESKTOP_REMOTE_STUB_UNAVAILABLE`.
- Remote-stub transport now supports HTTP health-probe wiring via `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_HEALTH_URL` with optional timeout/status-code controls (`PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_TIMEOUT_MS`, `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_ALLOWED_STATUS_CODES`) plus reason override `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BLOCK_REASON`.
- Remote-stub transport now supports backend execution wiring via `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BACKEND_BASE_URL` with optional timeout/auth/reason controls (`PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BACKEND_TIMEOUT_MS`, `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BACKEND_AUTH_TOKEN`, `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BACKEND_BLOCK_REASON`) plus operation-scoped auth endpoint overrides (`PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BACKEND_ENDPOINT_SET_REMEMBER_SESSION`, `..._SIGN_IN`, `..._RESTORE_SESSION`, `..._REFRESH_TOKEN`) and transport-label visibility suffix (`http-backend-overrides:N`).
- Scripted transport-level operation blocking remains available via `PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BLOCKED_OPERATIONS` when HTTP health-probe URL is not configured.
- Remote-stub transport request contract now carries workflow/method/endpoint/payload metadata per operation so transport probes and future backend executors can validate request-shape parity.
- Backend execution failures now propagate operation-scoped remote-stub status text to workflow state surfaces via transport denial status.
- Backend execution success payloads can now drive workflow state transitions across auth/project/file/canvas/assets/collaboration/inspect/export/diagnostics remote-stub adapters, with delegate mutation fallback preserved when backend state payload is absent.
- Backend response parser now supports nested success envelopes (`result`, `data`, `payload`) including deep envelope-chain traversal with cycle-safe guards, cyclic-primary-wrapper alternate-key fallback, sibling-envelope candidate fallback across wrapper scopes, and status aliases (`message`, `detail`) for response-driven state transitions.
- Desktop shell route/state bridge now supports initial section-route seeding via `PENJAR_DESKTOP_INITIAL_SECTION` and selected-section restoration across restart (`shell_contract_persistence_parity_test.dart`).
- Desktop shell launch routing now supports deep-link/route argument parsing via `--penjar-section=...`, `--penjar-route=...`, and `penjar://...` section-route payloads.
- Desktop shell now includes host launch-route channel bridge (`penjar/desktop/launch_route`: `consumeLaunchRoute` + `onLaunchRoute`) with runtime host-pushed route event handling coverage, plus macOS URL-scheme registration and Windows running-instance route relay (`WM_COPYDATA` -> `onLaunchRoute`) baselines for `penjar://` deep-link handoff.
- Windows installer flow now includes protocol registration command-hook baseline (`PENJAR_WINDOWS_PROTOCOL_REGISTER_COMMAND`) with optional `PENJAR_WINDOWS_PROTOCOL_SCHEME` / `PENJAR_WINDOWS_PROTOCOL_TARGET_PATH` overrides, strict gate control (`STRICT_WINDOWS_PROTOCOL_REGISTRATION`), and helper script template (`desktop/scripts/register_windows_protocol.ps1`); real production registry/installer command provisioning remains pending.
- Remote-stub auth flow now includes auth snapshot store/seed seam, including startup auth-state seed parsing via `PENJAR_DESKTOP_REMOTE_STUB_AUTH_STATE_JSON`.
- Remote-stub auth flow now supports flutter_secure_storage-backed native credential-store adapter path with default-on rollout (`PENJAR_DESKTOP_REMOTE_STUB_AUTH_SECURE_STORAGE_ENABLED` unset) plus explicit opt-out (`PENJAR_DESKTOP_REMOTE_STUB_AUTH_SECURE_STORAGE_ENABLED=false`) and optional key override `PENJAR_DESKTOP_REMOTE_STUB_AUTH_SECURE_STORAGE_KEY`.
- Secure-store rollout controls include strict read-fallback mode (`PENJAR_DESKTOP_REMOTE_STUB_AUTH_SECURE_STORAGE_STRICT`) in runtime auth-store resolution.
- Legacy auth-store command/file/mirror env inputs are runtime-decommissioned for auth-store resolution (`PENJAR_DESKTOP_REMOTE_STUB_AUTH_STATE_PATH`, `PENJAR_DESKTOP_REMOTE_STUB_AUTH_STATE_LOAD_COMMAND`, `PENJAR_DESKTOP_REMOTE_STUB_AUTH_STATE_SAVE_COMMAND`, `PENJAR_DESKTOP_REMOTE_STUB_AUTH_SECURE_STORAGE_MIRROR_LEGACY`).
- Legacy auth-store decommission guard automation is available via `pnpm run desktop:auth-store:legacy-decommission:check` and `pnpm run desktop:auth-store:legacy-decommission:contract:check`, with strict enforcement (`STRICT_AUTH_STORE_LEGACY_DECOMMISSION=1`) in verification baseline.
- Runtime source-level auth-store decommission guard automation is available via `pnpm run desktop:auth-store:runtime-decommission:check` and `pnpm run desktop:auth-store:runtime-decommission:contract:check`.
- Remote-stub auth backend response parsing now normalizes flat+nested alias payloads for remember/signed-in fields (`remember`, `persistSession`, `remember_session`, `persist_session`, `isAuthenticated`, `authenticated`, `signed_in`, `is_authenticated`) plus explicit signed-out state aliases (`signedOut`, `isSignedOut`, `loggedOut`, `isLoggedOut`, `signed_out`, `is_signed_out`, `logged_out`, `is_logged_out`), and token/session/user hints (`accessToken`, `token`, `sessionToken`, `refreshToken`, `sessionId`, `user`) across `auth`/`authentication`, `session`, and `tokens` payloads, with explicit signed-out override precedence and flat+nested signed-out backend error-code precedence (`AUTH_REQUIRED`, `TOKEN_EXPIRED`, etc.) including `error`/`errors`/`failure` container payloads, numeric unauthorized/session-expiry codes (`401`, `403`, `419`, `440`), status-code alias extraction support (`statusCode`, `httpStatus`, `status_code`, `http_status`) with camelCase/snake_case regression coverage, failure-flag alias coverage (`ok`, `isSuccess`, `isOk`, `is_success`, `is_ok`) plus mixed-alias explicit-false precedence coverage (`success=true`, `is_success=false`, including nested `errors[].is_success=false`), nested error/error-list status-detail message normalization, deterministic code-only/failure-only fallback status mapping (`Authentication required.` / `Backend session expired.` / `Backend auth request failed.`) when explicit status/message/detail is absent, expanded session-expiry code variant normalization (`SESSION_TIMEOUT`, `EXPIRED_TOKEN`), allocation-light backend code normalization usage for classifier hot paths, single-pass backend code classification reuse for state/status inference, centralized auth alias constant reuse for parser drift-prevention, cycle-safe deep envelope-chain traversal (including cyclic envelope-reference guard behavior and cyclic-primary-wrapper alternate-key fallback), sibling-envelope candidate fallback for metadata-only primary wrappers, and explicit signed-in precedence regression coverage for code/failure-flag/state-alias variants.
- Auth parser now treats explicit backend failure flags (`success`, `ok`, `isSuccess`, `isOk`, `is_success`, `is_ok`) set to false as signed-out inference precedence when explicit signed-in aliases are absent, including nested `error`/`meta` container variants and mixed-alias collisions where one failure flag is truthy but another is explicitly false.
- Signed-out precedence now forces signed-out transitions from previously signed-in snapshots when backend signed-out error-code or explicit failure-flag signals are present without explicit signed-in aliases.
- Auth contract now supports strict malformed-backend-auth schema fallback gate (`RemoteStubAuthSessionContract.strictBackendSchema`) that blocks delegate fallback when backend payload is present but does not satisfy auth-state parser schema, with strict malformed-payload auth parity UI coverage in `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Desktop contract bundle now forwards strict backend-auth schema mode through `DesktopContractBundle.fromMode(...)`, with environment toggle support via `PENJAR_DESKTOP_REMOTE_STUB_AUTH_BACKEND_SCHEMA_STRICT` in `fromEnvironment()` and `loadFromEnvironment()`.
- Auth contract now supports optional required backend-auth state mode (`RemoteStubAuthSessionContract.requireBackendState`) that blocks delegate fallback when backend auth payload is missing/malformed, with runtime toggle `PENJAR_DESKTOP_REMOTE_STUB_AUTH_BACKEND_REQUIRE_STATE` (auto-enabled by default for backend execution transport, explicit `false` opt-out supported).
- Desktop auth transport payload policy now supports optional sign-in credential forwarding via `PENJAR_DESKTOP_REMOTE_STUB_AUTH_BACKEND_FORWARD_CREDENTIALS`; default behavior remains sanitized (`passwordLength` without raw password).
- HTTP backend auth non-2xx responses are now normalized into auth payload snapshots (`code`/`message`) so parser-driven auth state/status handling runs instead of transport-only block surfacing.
- Runtime remote profile summary now surfaces sign-in payload forwarding mode as `auth-sign-in-payload: forwarded` when credential forwarding is enabled.
- Runtime remote profile summary now surfaces strict auth backend schema mode as `auth-backend-schema: strict` for diagnostics visibility, with parity UI coverage in `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Runtime remote profile summary now surfaces required backend-auth state mode as `auth-backend-state: required` and backend fallback-policy mode labels as `auth-backend-fallback: require-state` / `auth-backend-fallback: delegate-enabled` / `auth-backend-fallback: strict-schema`, with parity UI coverage in `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Parity UI coverage now includes backend unauthorized refresh transition from prior signed-in status in `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Parity UI coverage now includes required backend-auth state mode fallback blocking behavior for empty sign-in backend responses in `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Parity UI coverage now includes auto-enabled required backend-auth state behavior for backend execution transport profiles in `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Contract bundle + parity coverage now includes strict-schema fallback-policy diagnostics label (`auth-backend-fallback: strict-schema`) in `desktop/test/contracts/desktop_contract_bundle_test.dart` and `desktop/test/parity/remote_stub_mode_parity_test.dart`.
- Auth-session parity suite now includes backend integration flow coverage in `desktop/test/parity/auth_session_parity_test.dart` (backend snapshot sign-in/signed-out transition + required-state fallback blocking + code-only backend failure status mapping + statusCode/status_code unauthorized mapping + multi-alias failure-flag (success / ok / isSuccess / is_success / is_ok / isOk) auth-failed mapping (including dedicated success=false parity lock and explicit signed-in override parity locks for success/ok/isSuccess/is_success/is_ok/isOk plus nested failure-flag containers, code/statusCode/status_code/auth-required/session-timeout/expired-token overrides, signedOut-alias collisions, and nested authentication error-code/error-list explicit signed-in alias collisions, nested authentication detail/top-level status precedence locks, and nested signed-out error-code message mapping locks, token/session alias signed-in inference locks, explicit signed-out-vs-token/session alias precedence locks, and nested auth/session/token alias normalization locks, and signed-in alias unauthorized-code override locks (signed_in / is_signed_in / is_logged_in / isLoggedIn), and nested explicit signed-out alias override token-inference locks, error-object code token-inference override locks, and errorCode/httpStatus/http_status/SESSION_TIMEOUT/EXPIRED_TOKEN/TOKEN_EXPIRED session-expired fallback mapping locks and explicit signed-in override locks and explicit message-over-code fallback precedence locks) + mixed-alias failure-flag explicit-false precedence mapping (`success=true`, `is_success=false`, including nested `errors[].is_success=false`) + snake_case + logged-style camelCase signed-in/remember alias normalization including dedicated `signed_in` + `is_signed_in` + `logged_in` + `is_logged_in` + `loggedIn` + `isLoggedIn` locks + full signed-out alias variant fallback-status mapping (`signedOut`, `isSignedOut`, `loggedOut`, `isLoggedOut`, `signed_out`, `is_signed_out`, `logged_out`, `is_logged_out`) + session-timeout session-expired mapping + payload-envelope wrapper parity coverage for `payload` and nested `result -> payload` paths + deep envelope-chain parity coverage for `result -> data -> payload -> result -> data` + cyclic-primary-wrapper alternate-key fallback parity coverage + sibling `result`/`data` fallback parity coverage for metadata-only primary wrappers + authState envelope alias parity locks (signed-in envelope aliases `result.authState.isAuthenticated`, `data.authState.isAuthenticated`, `data.authState.loggedIn`, `result.authState.loggedIn`, `data.authState.isLoggedIn`, `result.authState.isLoggedIn`, `data.authState.is_authenticated`, `result.authState.is_authenticated`, `data.authState.signedIn`, `result.authState.signedIn`, `data.authState.authenticated`, `result.authState.authenticated`, `data.authState.logged_in`/`result.authState.logged_in`, `data.authState.is_logged_in`/`result.authState.is_logged_in`, `data.authState.signed_in`/`result.authState.signed_in`, `data.authState.is_signed_in`/`result.authState.is_signed_in`, `result.authState.signedOut`/`signed_out`/`isSignedOut`/`loggedOut`/`logged_out`/`isLoggedOut`, `data.authState.signedOut`/`signed_out`/`isSignedOut`/`loggedOut`/`logged_out`/`isLoggedOut`, `data.authState.is_signed_out`/`is_logged_out`, `result.authState.is_signed_out`/`is_logged_out`, and `data.authState.authentication.authenticated=false` token-collision precedence with deterministic signed-out fallback status (`Authentication required.`), plus explicit signed-in-false envelope aliases (`result.authState.isAuthenticated=false`, `data.authState.isAuthenticated=false`, `data.authState.loggedIn=false`, `result.authState.loggedIn=false`, `result.authState.isLoggedIn=false`, `data.authState.isLoggedIn=false`, `data.authState.is_authenticated=false`, `result.authState.is_authenticated=false`, `result.authState.signedIn=false`, `data.authState.signedIn=false`, `data.authState.authenticated=false`, `result.authState.authenticated=false`, `result.authState.signed_in=false`, `data.authState.signed_in=false`, `result.authState.is_signed_in=false`, `data.authState.is_signed_in=false`, `result.authState.logged_in=false`, `data.authState.logged_in=false`, `data.authState.is_logged_in=false`, `result.authState.is_logged_in=false`))).
- Auth backend contract fixture matrix baseline is now maintained in `desktop/test/contracts/workflow_contracts_test.dart` (`auth backend contract fixture: ...` cases), including `result/data/payload` envelope-chain + `authState` alias coverage, deep envelope-chain traversal fixture coverage beyond prior bounded depth, cyclic envelope-reference guard fixture coverage and cyclic-primary-wrapper alternate-key fallback fixture coverage, sibling `result`/`data` fallback fixture coverage for metadata-only primary wrappers, status-code alias extraction coverage (`statusCode` / `httpStatus` / `status_code` / `http_status`), explicit signed-out envelope alias fixture coverage (`signedOut`, `signed_out`, `isSignedOut`, `is_signed_out`), explicit signed-in envelope fixtures (`result.authState.isAuthenticated`, `data.authState.isAuthenticated`, `data.authState.loggedIn`, `result.authState.loggedIn`, `data.authState.isLoggedIn`, `result.authState.isLoggedIn`, `data.authState.is_authenticated`, `result.authState.is_authenticated`, `data.authState.signedIn`, `result.authState.signedIn`, `data.authState.authenticated`, `result.authState.authenticated`, `data.authState.logged_in`, `result.authState.logged_in`, `data.authState.is_logged_in`, `result.authState.is_logged_in`, `data.authState.signed_in`, `result.authState.signed_in`, `data.authState.is_signed_in`, `result.authState.is_signed_in`), logged-style signed-out alias regression coverage (`loggedOut`, `isLoggedOut`, `logged_out`, `is_logged_out`), and explicit signed-in precedence regression cases for `SESSION_TIMEOUT` / `EXPIRED_TOKEN`, statusCode/status_code unauthorized variants, multi-alias failure-flag (success / ok / isSuccess / is_success / is_ok / isOk) variants, mixed-alias failure-flag explicit-false precedence variant (`success=true`, `is_success=false`, including nested `errors[].is_success=false`), and explicit signedIn vs signedOut/isSignedOut/loggedOut/isLoggedOut/signed_out/is_signed_out/logged_out/is_logged_out state-alias collisions, to lock normalized signed-in/status precedence behavior before real backend auth contract binding.
- Latest auth fixture/parity expansion (`Unit WS-D-230`) completes refresh-token direct-wrapper signed-out alias symmetry for `result/data.authState` (`signedOut`, `signed_out`, `isSignedOut`, `loggedOut`, `logged_out`, `isLoggedOut`, `is_signed_out`, `is_logged_out`) in contract and parity suites; see execution log evidence (`Unit WS-D-229`, `Unit WS-D-230`).
- Latest auth fixture/parity expansion (`Unit WS-D-231`) completes refresh-token direct-wrapper explicit signed-in-false alias symmetry for `result/data.authState` (`isAuthenticated`, `loggedIn`, `isLoggedIn`, `is_authenticated`, `signedIn`, `authenticated`, `signed_in`, `is_signed_in`, `logged_in`, `is_logged_in`) in contract and parity suites; see execution log evidence.
- Latest auth fixture/parity expansion (`Unit WS-D-232`) completes refresh-token direct-wrapper signed-in alias success symmetry for `result/data.authState` (`isAuthenticated`, `loggedIn`, `isLoggedIn`, `is_authenticated`, `signedIn`, `authenticated`, `signed_in`, `is_signed_in`, `logged_in`, `is_logged_in`) in contract and parity suites; see execution log evidence.
- Latest auth fixture/parity expansion (`Unit WS-D-233`) completes restore-session direct-wrapper signed-in alias success symmetry for `result/data.authState` (`isAuthenticated`, `loggedIn`, `isLoggedIn`, `is_authenticated`, `signedIn`, `authenticated`, `signed_in`, `is_signed_in`, `logged_in`, `is_logged_in`) in contract and parity suites; see execution log evidence.
- Latest auth fixture/parity expansion (`Unit WS-D-234`) completes sign-in direct-wrapper explicit signed-in-false alias symmetry for `result/data.authState` (`isAuthenticated`, `loggedIn`, `isLoggedIn`, `is_authenticated`, `signedIn`, `authenticated`, `signed_in`, `is_signed_in`, `logged_in`, `is_logged_in`) in contract and parity suites; see execution log evidence.
- Latest auth fixture/parity expansion (`Unit WS-D-235`) completes sign-in direct-wrapper signed-out alias symmetry for `result/data.authState` (`signedOut`, `signed_out`, `isSignedOut`, `loggedOut`, `logged_out`, `isLoggedOut`, `is_signed_out`, `is_logged_out`) in contract and parity suites; see execution log evidence.
- Latest auth fixture expansion (`Unit WS-D-236`) closes residual restore-session direct `result.authState` signed-out alias fixture gaps (`signedOut`, `loggedOut`) so result/data wrapper fallback matrix coverage remains symmetric; see execution log evidence.
- Latest auth fixture expansion (`Unit WS-D-237`) adds an automated fixture-matrix guard in `desktop/test/contracts/workflow_contracts_test.dart` to enforce direct `authState` alias symmetry across `sign-in`/`refresh-token`/`restore-session` and `result/data` wrappers for signed-in/signed-out alias sets; see execution log evidence.
- Latest auth parity expansion (`Unit WS-D-238`) adds parity-side authState matrix guards in `desktop/test/parity/auth_session_parity_test.dart` and closes residual restore-session direct `result.authState` signed-out parity gaps (`signedOut`, `loggedOut`); see execution log evidence.
- Latest auth parity expansion (`Unit WS-D-239`) adds parity-side matrix-guard coverage for restore-session/refresh-token direct `result/data.authState` signed-in-false (`false`) alias symmetry across the full signed-in alias set; see execution log evidence.
- Latest auth parity expansion (`Unit WS-D-240`) deduplicates restore-session/refresh-token direct `authState` matrix guards via shared helper + prefiltered payload reuse, preserving deterministic missing-entry reporting while reducing duplicate assertion paths; see execution log evidence.
- Latest auth parity expansion (`Unit WS-D-241`) deduplicates sign-in direct `authState` matrix guards across signed-in/signed-in-false/signed-out paths via shared helper reuse while preserving deterministic missing-entry reporting; see execution log evidence.
- Latest auth parity expansion (`Unit WS-D-242`) consolidates cross-operation authState matrix assertion plumbing through shared append/expect helpers reused by sign-in/refresh-token/restore-session and restore/refresh combined guards, preserving deterministic missing-entry reporting while reducing repeated wrapper/alias loops; see execution log evidence.
- Latest auth parity expansion (`Unit WS-D-243`) adds authState matrix lookup performance indexing (per-payload-set entry set + keyed membership checks) under shared guard helpers, preserving deterministic missing-entry reporting while reducing repeated payload scans; see execution log evidence.
- Latest auth parity/contract alignment expansion (`Unit WS-D-244`) applies keyed authState matrix entry indexing plus shared missing-entry helper reuse to the contract-side fixture symmetry guard in `desktop/test/contracts/workflow_contracts_test.dart`, preserving deterministic missing-entry reporting while reducing repeated fixture scans in contract matrix validation; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-245`) consolidates backend code signed-out/session-expired checks into a single classification path in `desktop/lib/contracts/remote_stub_contracts.dart`, preserving existing signed-out/session-expired semantics while removing duplicate backend-code compact/marker scans during auth-state inference; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-246`) adds numeric backend-code fast paths (`401`/`403`/`419`/`440`) plus empty compact-input short-circuiting in `_classifyBackendCode(...)`, preserving signed-out/session-expired semantics while reducing marker-loop overhead on common status paths; see execution log evidence.
- Latest auth parser coverage expansion (`Unit WS-D-247`) adds `JWT_EXPIRED` normalization (`jwtexpired`) to signed-out/session-expired code classification and locks both deterministic session-expired fallback and explicit signed-in override precedence in contract/parity suites; see execution log evidence.
- Latest auth parser coverage expansion (`Unit WS-D-248`) makes `session-expired` marker inclusion in `signed-out` classification structural (`_backendSignedOutCodeMarkers` includes `_backendSessionExpiredCodeMarkers`) and locks `ACCESS_TOKEN_EXPIRED`/`REFRESH_TOKEN_EXPIRED` deterministic session-expired fallback plus explicit signed-in override precedence in contract/parity suites; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-249`) adds exact-marker set fast paths for signed-out/session-expired code catalogs in `_classifyBackendCode(...)`, preserving existing fallback semantics while reducing substring-loop work for common compact backend code values; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-250`) adds minimum-marker-length short-circuits for signed-out/session-expired code catalogs in `_classifyBackendCode(...)`, preserving exact-marker/composite fallback behavior while reducing substring-loop work for impossible short compact code inputs; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-251`) removes duplicate trim work in classifier code normalization by compacting from classifier-local trimmed input (`_compactBackendCodeFromTrimmed(trimmed)`), preserving numeric/exact/substring fallback semantics while reducing preprocessing overhead; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-252`) replaces regex-based compact-code normalization in `_compactBackendCodeFromTrimmed(...)` with allocation-light character scanning, and locks delimited `REFRESH-TOKEN::EXPIRED` deterministic session-expired fallback coverage in contract/parity suites while preserving numeric/exact/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-253`) adds an already-compact lowercase fast path in `_compactBackendCodeFromTrimmed(...)` to skip `toLowerCase` preprocessing for compact inputs, and locks compact `refreshtokenexpired` deterministic session-expired fallback coverage in contract/parity suites while preserving numeric/exact/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-254`) unifies exact-marker classification through `_backendExactCodeClassifications` single-map lookup in `_classifyBackendCode(...)`, and locks compact `unauthorized` deterministic auth-required fallback coverage in contract/parity suites while preserving numeric/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-255`) reorders substring classification to scan session-expired markers first and signed-out-only markers second in `_classifyBackendCode(...)`, reducing duplicate session-marker scans while locking mixed `UNAUTHORIZED_REFRESH_TOKEN_EXPIRED` deterministic session-expired fallback coverage in contract/parity suites; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-256`) adds an uppercase-compact fast path in `_compactBackendCodeFromTrimmed(...)` to return direct lowercase output for fully ASCII alphanumeric uppercase inputs (skipping delimiter-filter buffer loops on compact-uppercase inputs), and locks uppercase compact `UNAUTHORIZED` deterministic auth-required fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-257`) adds non-compact ASCII code-unit normalization in `_compactBackendCodeFromTrimmed(...)` to avoid full-string `toLowerCase` allocation for delimiter-heavy ASCII inputs while preserving non-ASCII normalization fallback behavior, and locks delimited uppercase `UNAUTHORIZED::TOKEN` deterministic auth-required fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-258`) removes separate non-ASCII pre-scan in `_compactBackendCodeFromTrimmed(...)` by using single-pass ASCII normalization with inline non-ASCII Unicode-fallback dispatch, and locks delimited uppercase `UNAUTHORIZED::토큰` deterministic auth-required fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-259`) adds uppercase compact exact-marker fast-path classification in `_classifyBackendCode(...)` via `_backendUppercaseExactCodeClassifications`, short-circuiting normalization for uppercase exact codes and locking uppercase compact `TOKENEXPIRED` deterministic session-expired fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-260`) expands raw exact-marker fast-path classification in `_classifyBackendCode(...)` to include both lowercase and uppercase compact exact codes via combined exact-map lookup before compact normalization, and locks lowercase compact `sessionexpired` deterministic session-expired fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-261`) consolidates raw exact-marker fast-path classification in `_classifyBackendCode(...)` to single-map lookup (`_backendRawExactCodeClassifications`) that pre-materializes lowercase/uppercase exact keys, and locks uppercase compact `SESSIONEXPIRED` deterministic session-expired fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-262`) replaces all-compact uppercase normalization in `_compactBackendCodeFromTrimmed(...)` with ASCII code-unit lowering helper (`_compactBackendAsciiLowercase(...)`), and locks mixed-case compact `SessionExpired` deterministic session-expired fallback coverage in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-263`) adds an early Unicode-fallback branch in `_compactBackendCodeFromTrimmed(...)` when the first non-compact character is non-ASCII, reducing redundant ASCII normalization scans while locking compact uppercase `UNAUTHORIZED토큰` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-264`) reuses the pre-scanned compact prefix in `_compactBackendCodeFromTrimmed(...)` before delimiter-driven normalization continues from `firstNonCompactIndex`, reducing duplicate prefix scan overhead while locking mixed-case delimited `Unauthorized::TOKEN` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-265`) appends Unicode-lowercased compact suffix units from the first non-ASCII index in `_compactBackendCodeFromTrimmed(...)` via incremental suffix normalization, avoiding full-string lowercase + delimiter-path rescans while locking mixed-case delimited `Unauthorized::TOKEN토큰` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-266`) replaces lowercase delimiter-path prefix `substring` allocation in `_compactBackendCodeFromTrimmed(...)` with `_appendCompactBackendAsciiRange(...)` prefix appends when no uppercase compact units exist, reducing transient allocation overhead while locking lowercase delimited `unauthorized::token` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-267`) skips rechecking the already-identified first delimiter index in `_compactBackendCodeFromTrimmed(...)` by continuing delimiter-path scanning from `firstNonCompactIndex + 1`, reducing redundant branch checks while locking lowercase trailing-delimiter `unauthorized::` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-268`) tracks `firstUppercaseCompactIndex` during compact no-delimiter normalization in `_compactBackendCodeFromTrimmed(...)` and lowercases only from that index onward through start-indexed helper ranges, reducing redundant lowercase-prefix rescans while locking lowercase-prefix uppercase-suffix compact `unauthorizedTOKEN` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-269`) reuses `firstUppercaseCompactIndex` during delimiter-path prefix normalization in `_compactBackendCodeFromTrimmed(...)` to append known-lowercase prefix segments unchanged and lowercase only `firstUppercaseCompactIndex..firstNonCompactIndex`, reducing redundant lowercase-prefix checks while locking lowercase-prefix uppercase-before-delimiter `unauthoriZed::token` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-270`) lazily allocates the compact-normalization `StringBuffer` in `_compactBackendCodeFromTrimmed(...)` only when compact output append is needed, reducing empty-buffer allocation overhead on delimiter-only paths while locking delimiter-only `code: "::"` signed-in/status stability behavior (no unintended auth-required/session-expired fallback mapping) in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-271`) replaces local lazy-buffer closure dispatch in `_compactBackendCodeFromTrimmed(...)` with inline `asciiBuffer ??= StringBuffer()` initialization across prefix/unicode/delimiter append paths, preserving lazy allocation semantics while reducing hot-loop closure overhead and locking unicode-only `code: "\uC138\uC158\uB9CC\uB8CC"` signed-in/status stability behavior (no unintended auth-required/session-expired fallback mapping) in contract/parity suites while preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-272`) reuses a loop-local output buffer (`outputBuffer`) for delimiter-path suffix scanning in `_compactBackendCodeFromTrimmed(...)`, reducing repeated append-buffer null-coalescing assignments on hot iterations while locking leading-delimiter uppercase `code: "::UNAUTHORIZED"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-273`) caches `codeLength` in `_compactBackendCodeFromTrimmed(...)` and reuses it across scan/suffix loops plus explicit compact-uppercase normalization boundaries (`endExclusive`), reducing repeated length getter lookups while locking capitalized compact `code: "Unauthorized"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-274`) uses index-based marker loops with cached marker counts and immediate session-expired marker returns in `_classifyBackendCode(...)`, reducing iterator overhead while locking capitalized compact `code: "Unauthenticated"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-275`) caches `compactLength` in `_classifyBackendCode(...)` for compact-empty and marker-min-length guards, reducing repeated length getter lookups while locking capitalized compact `code: "LoggedOut"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-276`) caches marker-list references in `_classifyBackendCode(...)` before marker index traversal, reducing repeated top-level list lookups while locking capitalized compact `code: "AuthRequired"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-277`) caches marker minimum-length thresholds as local values in `_classifyBackendCode(...)` before threshold guards, reducing repeated top-level threshold lookups while locking capitalized compact `code: "SignedOut"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-278`) applies short-code early-return guards in `_classifyBackendCode(...)` before raw-exact lookup/compact normalization when marker-length matching is impossible, reducing unnecessary classifier work while locking capitalized compact near-miss `code: "SignOut"` signed-in/status stability behavior (no unintended auth-required fallback) in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-279`) adds long-code exact-lookup guards in `_classifyBackendCode(...)` by skipping raw/compact exact-map probes when input length exceeds combined signed-out marker max length, reducing unnecessary exact-map lookups while locking capitalized compact suffix `code: "SignedOutSessionStateMismatch"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-280`) reuses static marker-count constants in `_classifyBackendCode(...)` for session-expired/signed-out-only index scans, reducing repeated per-call marker-length reads while locking capitalized compact mixed-marker `code: "UnauthorizedSessionTimeoutContinuation"` deterministic session-expired fallback precedence coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-281`) gates raw/compact exact-map probes in `_classifyBackendCode(...)` by precomputed marker-length catalog membership, reducing unnecessary exact-map lookups on non-catalog lengths while locking capitalized compact signed-out length-gap suffix `code: "SignedOutErr"` deterministic auth-required fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Latest auth parser performance expansion (`Unit WS-D-282`) replaces set-based exact-length candidate checks in `_classifyBackendCode(...)` with bitmask membership guards for raw/compact exact-map probes, reducing exact-gating overhead while locking capitalized compact JWT-expired suffix `code: "JwtExpiredErr"` deterministic session-expired fallback coverage in contract/parity suites and preserving numeric/exact/marker-length/substring fallback semantics; see execution log evidence.
- Backend auth staged rollout/decommission criteria baseline is now codified in [Desktop Flutter Auth Backend Contract Integration Plan](/technical-guide/developer/desktop-flutter-auth-backend-contract-integration-plan/) (`R0..R4` stages + decommission checklist); parity acceptance remains `In progress` until per-stage execution evidence is published.
- Additional auth contract regressions now include snake_case + logged-style camelCase signed-in/remember alias normalization, full signed-out alias variant fallback-status mapping, and `signed_in` override precedence under unauthorized code payloads.
- Environment-driven blocked-operation lists are filtered against `RemoteStubOperationIds` catalog in `desktop/lib/contracts/remote_stub_contracts.dart` to avoid unsupported operation keys.
- Diagnostics parity surface now includes runtime remote profile summary text (`Remote profile: ...`) including `auth-store` label for active auth snapshot persistence mode visibility during degraded-path validation.
