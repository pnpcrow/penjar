---
title: Desktop Flutter Parity Checklist
desc: Workflow-level parity checklist for the Flutter desktop full-port program.
---

# Desktop Flutter Parity Checklist

This checklist tracks user-facing workflow parity for the Flutter desktop full-port mandate.

## Related artifacts

- [Web + MCP + Desktop Documentation Map](/technical-guide/developer/web-mcp-documentation-map/)
- [Web + MCP + Desktop Delivery Roadmap](/technical-guide/developer/web-mcp-desktop-roadmap/)
- [Web + MCP + Desktop Detailed Implementation Plan](/technical-guide/developer/web-mcp-desktop-implementation-plan/)
- [Web + Desktop Phase C Execution Log](/technical-guide/developer/web-mcp-phase-c-execution-log/)
- [Desktop Flutter Development Runbook](/technical-guide/developer/desktop-flutter-development-runbook/)
- [Desktop Flutter Auth Backend Contract Integration Plan](/technical-guide/developer/desktop-flutter-auth-backend-contract-integration-plan/)
- [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/)
- [Desktop Flutter Parity Acceptance Baseline](/technical-guide/developer/desktop-flutter-parity-acceptance-baseline/)
- [Desktop Flutter Release Validation Baseline](/technical-guide/developer/desktop-flutter-release-validation-baseline/)

## Checklist legend

- **Not started**: no Flutter implementation evidence yet.
- **In progress**: partial implementation or test evidence exists.
- **Done**: Flutter implementation + tests + diagnostics + docs completed.
- **Blocked**: temporary non-Flutter path with explicit blocker/owner/removal date.

## Workflow checklist

| Workflow domain | Representative workflow | Status | Owner | Evidence link | Notes |
|---|---|---|---|---|---|
| Authentication/session | Sign in, session restore, token refresh | In progress | Auth + Desktop Integration | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter auth/session parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend request metadata mapping, backend execution-path error propagation, backend response-driven state mutation wiring, auth snapshot store/seed seam, flutter_secure_storage-backed native credential-store adapter path, secure-store strict rollout/fallback controls, default-on secure-store rollout telemetry labels, runtime legacy auth-store command/file/mirror path decommission, strict decommission guard automation, runtime source-level decommission guard automation, backend auth normalization (flat+nested auth/session/token alias handling with snake_case signed-in/remember aliases (`signed_in` / `is_authenticated` / `logged_in` / `is_logged_in` / `remember_session` / `persist_session`) + explicit signed-out aliases (`signedOut` / `isSignedOut` / `loggedOut` / `isLoggedOut` / `signed_out` / `is_signed_out` / `logged_out` / `is_logged_out`) + explicit signed-out and flat+nested signed-out error-code precedence including error/error-list containers + numeric unauthorized/session-expiry codes + nested error/error-list status-detail message normalization + explicit failure-flag precedence including nested failure-flag containers + failure-flag aliases (`ok` / `isSuccess` / `isOk` / `is_success` / `is_ok`) + mixed-alias explicit-false precedence coverage (`success=true` + `is_success=false`, including nested `errors[].is_success=false`) + signed-out signal forced transition from previously signed-in snapshots when explicit signed-in aliases are absent + deterministic fallback status mapping for code-only/failure-only backend auth signals: `Authentication required.` / `Backend session expired.` / `Backend auth request failed.` with explicit status/message precedence + expanded session-expiry code variant handling including `SESSION_TIMEOUT` / `EXPIRED_TOKEN` + backend status-code alias extraction support (`statusCode` / `httpStatus` / `status_code` / `http_status`) with camelCase/snake_case regression coverage + cached backend code normalization regex helper to reduce repeated classifier allocation overhead + single-pass backend code classification reuse for state/status inference paths + centralized auth alias constant reuse for parser drift-prevention + explicit signed-in override regression coverage for those variants + cycle-safe recursive failure/status/code container traversal guards for malformed cyclic payloads), strict malformed-backend-auth schema fallback gate (`RemoteStubAuthSessionContract.strictBackendSchema`) with strict malformed-payload auth parity UI coverage (`desktop/test/parity/remote_stub_mode_parity_test.dart`), runtime bundle strict-schema wiring (`DesktopContractBundle.fromMode` + `PENJAR_DESKTOP_REMOTE_STUB_AUTH_BACKEND_SCHEMA_STRICT` via `fromEnvironment`/`loadFromEnvironment`), optional required backend-state runtime gate (`PENJAR_DESKTOP_REMOTE_STUB_AUTH_BACKEND_REQUIRE_STATE`) with diagnostics-visible profile labels (`auth-backend-state: required`, `auth-backend-fallback: require-state`) and parity UI fallback-block coverage plus delegate/strict fallback label visibility (auto-enabled by default when backend execution transport is configured, explicit `false` opt-out supported), optional sign-in credential-forwarding runtime toggle (`PENJAR_DESKTOP_REMOTE_STUB_AUTH_BACKEND_FORWARD_CREDENTIALS`) with default sanitized payload policy (`passwordLength`) and diagnostics-visible forwarding profile label (`auth-sign-in-payload: forwarded`), operation-scoped auth backend endpoint override wiring (`PENJAR_DESKTOP_REMOTE_STUB_TRANSPORT_BACKEND_ENDPOINT_SET_REMEMBER_SESSION` / `SIGN_IN` / `RESTORE_SESSION` / `REFRESH_TOKEN`) with diagnostics-visible transport label suffix (`http-backend-overrides:N`), HTTP backend auth non-2xx response normalization into auth payload snapshots (`code`/`message`) for parser-driven state/error handling, diagnostics-visible strict-schema profile label (`auth-backend-schema: strict`) with parity UI coverage (`desktop/test/parity/remote_stub_mode_parity_test.dart`), auth-session parity backend integration coverage in `desktop/test/parity/auth_session_parity_test.dart` (backend snapshot sign-in/signed-out transition + required-state fallback blocking + code-only failure mapping status coverage + statusCode unauthorized mapping coverage + status_code unauthorized mapping coverage + multi-alias failure-flag (success / ok / isSuccess / is_success / is_ok / isOk) auth-failed mapping coverage (including dedicated success=false parity lock and explicit signed-in override parity locks for success/ok/isSuccess/is_success/is_ok/isOk plus nested failure-flag containers, code/statusCode/status_code/auth-required/session-timeout/expired-token overrides, and signedOut-alias collisions) + mixed-alias failure-flag false-precedence mapping coverage (`success=true` + `is_success=false`, including nested `errors[].is_success=false`) + snake_case + logged-style camelCase signed-in/remember alias normalization coverage including dedicated `logged_in` + `is_logged_in` + `loggedIn` + `isLoggedIn` locks + full signed-out alias variant fallback-status coverage (`signedOut` / `isSignedOut` / `loggedOut` / `isLoggedOut` / `signed_out` / `is_signed_out` / `logged_out` / `is_logged_out`) + session-timeout session-expired mapping coverage + payload-envelope wrapper parity coverage for `payload`, nested `result -> payload`, and deep `result -> data -> payload -> result -> data` paths, plus cyclic-primary-envelope alternate-wrapper fallback coverage and sibling `result`/`data` fallback coverage for metadata-only primary chains), and auth backend contract fixture matrix baseline (including `result/data/payload` envelope-chain coverage (including deep-chain traversal beyond prior bounded depth and cyclic envelope-reference safety guards and cyclic-primary-wrapper alternate-path fallback checks) + `authState` alias coverage with explicit signed-in fixtures (`data.authState.loggedIn`, `result.authState.isLoggedIn`) and logged-style signed-out fallback fixtures (`result.authState.loggedOut`, `result.authState.isLoggedOut`, `data.authState.is_logged_out`), plus explicit signed-in precedence regression cases for `SESSION_TIMEOUT` / `EXPIRED_TOKEN`, statusCode unauthorized variant, status_code unauthorized variant, multi-alias failure-flag (success / ok / isSuccess / is_success / is_ok / isOk) signed-in precedence variants, mixed-alias failure-flag explicit-false precedence variant (`success=true` + `is_success=false`, including nested `errors[].is_success=false`), and explicit signedIn vs signedOut/isSignedOut/loggedOut/isLoggedOut/signed_out/is_signed_out/logged_out/is_logged_out alias precedence) are added; staged rollout/decommission criteria baseline is now codified, and per-stage rollout execution evidence plus full backend contract integration are pending and tracked in [Desktop Flutter Auth Backend Contract Integration Plan](/technical-guide/developer/desktop-flutter-auth-backend-contract-integration-plan/). |
| Project lifecycle | Open project, switch files/pages | In progress | Workspace Navigation | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter project lifecycle parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; backend contract/navigation integration is pending. |
| File lifecycle | Create/rename/open/delete file/page | In progress | Workspace Navigation | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter file lifecycle parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; repository/file API integration is pending. |
| Canvas editing | Create/move/resize/style shapes | In progress | Workspace Core | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter canvas parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; real renderer/contract integration is pending. |
| Asset management | Import/use assets | In progress | Workspace Core | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter asset-management parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; shared asset contract integration is pending. |
| Collaboration context | Threads/presence awareness | In progress | Collaboration + Realtime | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter collaboration parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; realtime backend integration is pending. |
| Inspect/code handoff | Inspect metadata extraction | In progress | Inspect/Handoff | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter inspect/handoff parity scaffold, parity test harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; inspect contract payload integration is pending. |
| Export workflows | PNG/SVG export and save | In progress | Export Pipeline | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter export-workflow parity scaffold, parity harness, runtime-switchable in-memory/remote-stub contract boundary, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; backend export pipeline/native save integration is pending. |
| Diagnostics/recovery | Runtime health, reconnect, remediation | In progress | Platform Reliability | [Desktop Flutter Migration Inventory](/technical-guide/developer/desktop-flutter-migration-inventory/) | Flutter diagnostics/recovery parity scaffold, parity harness, runtime-switchable in-memory/remote-stub contract boundary, HTTP health-probe remote transport gating, operation-level backend request metadata mapping, backend execution-path status propagation, backend response-driven state mutation wiring, and sibling `result`/`data` envelope fallback regression coverage (contract + parity) are added; live telemetry/reconnect policy integration is pending. |

## Cross-cutting shell/release routing note

- Windows deep-link runtime handoff is covered by running-instance relay (`WM_COPYDATA` -> `onLaunchRoute`) and installer-pipeline protocol registration command-hook baseline (`PENJAR_WINDOWS_PROTOCOL_REGISTER_COMMAND` with scheme/target overrides), with strict gate control (`STRICT_WINDOWS_PROTOCOL_REGISTRATION`) and helper script template (`desktop/scripts/register_windows_protocol.ps1`) available for release-smoke policy enforcement; production registry/installer command provisioning remains tracked as pending.

## Completion criteria per row

1. User-facing workflow path is implemented in Flutter desktop surface.
2. Equivalent acceptance tests exist (unit/integration/e2e where appropriate).
3. Operational diagnostics/recovery behavior is documented.
4. Linked execution evidence is recorded in Phase C execution log.
